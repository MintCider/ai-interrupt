{
  "name": "ai-interrupt",
  "version": "0.5.0",
  "repository": "https://github.com/MintCider/ai-interrupt",
  "license": "MIT",
  "scripts": {
    "build": "cross-env NODE_ENV=production node tools/build.js",
    "build-dev": "cross-env NODE_ENV=dev node tools/build.js"
  },
  "engines": {
    "node": ">=10"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.24.0"
  }
}
